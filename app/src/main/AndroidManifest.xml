<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javalon.cklite">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".airtime.PurchaseAirtimeActivity"
            android:label="@string/purchase_airtime"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".dataBundle.PurchaseDataBundleActivity"
            android:label="@string/purchase_data_bundle"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".cableTV.PurchaseCableTVActivity"
            android:parentActivityName=".MainActivity"
            android:label="@string/purchase_cable_tv"></activity>
    </application>

</manifest>